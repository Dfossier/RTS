using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TerrainDataDebug : MonoBehaviour
{
    /*
    public static List<MeshData> meshDatasList;

    private void Start()
    {
        Invoke("ListTerrainDatas", 2.0f);
        meshDatasList = new List<MeshData>();
    }
    // Start is called before the first frame update
    public static void AddMeshDataToList(MeshData md)
    {
        meshDatasList.Add(md);
    }

    public static void RemoveMeshDataToList(MeshData md)
    {
        meshDatasList.Remove(md);
    }

    void ListTerrainDatas()
    {
        foreach (MeshData md in TerrainDataDebug.meshDatasList)
        {
            print(md);
        }
    }
    */
}
